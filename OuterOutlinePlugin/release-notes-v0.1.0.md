# OuterOutlinePlugin v0.1.0

独立外轮廓线提取插件初始版本。

## 功能

- `PCOUTLINE`：从候选图层中提取楼层外轮廓闭合线
- `PCLOG`：显示诊断日志路径
- 连续点选图层样本并实时高亮参与对象
- 支持 `Largest` 和 `AllLarge` 两种生成模式
- 支持 `LINE`、`LWPOLYLINE`、`ARC`、`HATCH` 边界

## 产物

- `OuterOutlinePlugin-v0.1.0-autocad2022.dll`
