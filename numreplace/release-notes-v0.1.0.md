# numreplace v0.1.0

首个版本。

## 新增

- 新增 `NUMREPLACE` 命令。
- 支持目标数值匹配方式：大于 `G`、等于 `E`、小于 `L`、区间 `R`。
- 支持选择文字图层并高亮确认。
- 支持框选需要处理的文字范围。
- 支持设置替换后的随机数区间。
- 支持选择是否保留原文字小数位数。
- 新增 `NUMREPLACELOG` 命令和 `%TEMP%\NUMREPLACE.log` 诊断日志。
