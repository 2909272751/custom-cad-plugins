# NumberTextHighlightPlugin v0.1.0

首个版本。

## 新增

- 新增 `NUMRED` 命令。
- 支持输入用于比较的数值。
- 支持选择文字图层并高亮确认。
- 支持框选需要检查的范围。
- 支持条件：大于 `G`、等于 `E`、小于 `L`，默认大于。
- 支持将满足条件的数字文字改成红色。
- 新增 `NUMREDLOG` 命令和 `%TEMP%\NUMRED.log` 诊断日志。
