# TextBoxSelectPlugin v0.1.0

首个版本。

## 新增

- 新增 `TXTBOXSEL` 命令。
- 支持选择并确认框线图层。
- 支持选择并确认文字图层。
- 支持框选/交叉框选识别范围。
- 支持识别框选范围内包含文字的闭合 `Polyline` 框。
- 支持 `DBText` 和 `MText`。
- 识别成功后自动把命中的框放入 AutoCAD 当前选择集。
