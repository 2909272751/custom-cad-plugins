# beamnumsel v0.2.0

- 重写识别流程：增加横向、竖向、全部三种识别方向。
- 横向梁线支持选择上方、下方、两侧；竖向梁线支持选择左侧、右侧、两侧。
- 增加最大识别距离输入，默认 `50`。
- 增加预览确认流程，可输入 `D` 调整距离并重新预览。
- 每个数字按投影重合和距离归属到最佳梁线，减少误选和漏选。
- 使用新版 DLL 前需要重启 AutoCAD 后重新 `NETLOAD`。
