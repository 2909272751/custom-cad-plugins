# beamcolor v0.1.0

- 新增 `BEAMCOLOR` 命令：按梁编号前缀批量修改梁编号文字和对应梁线颜色。
- 颜色来自用户选择的样本梁编号文字。
- 支持多条前缀规则，例如 `KL`、`L`、`LLK`。
- `L` 只匹配 `L + 数字`，不会误匹配 `LLK`。
- 只修改梁编号文字，不修改截面尺寸和配筋文字。
- 新增 `BEAMCOLORLOG` 日志命令，日志写入 `%TEMP%\BEAMCOLOR.log`。
